#! /bin/bash

# 課題29:整数値を5回入力させ、それらの値の合計を表示するプログラムを繰り返しを使って作成せよ。

# sum=0
# for ((i = 0; i < 5; i++)); do
#     read -r -p "整数を5回入力してもらおうか: " num${i}
#     ((sum+=num${i}))
#     echo "いまのところ、合計は${sum}です"
# done
# echo "最終的な合計は${sum}です"